[project]
name = "airportgap-api-test"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "allure-pytest>=2.15.3",
    "httpx>=0.28.1",
    "pytest>=9.0.2",
    "pytest-xdist>=3.8.0",
]
